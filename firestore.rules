rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Open access - for development only!
    // Revert to stricter rules before going to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
